index_generator:
  exclude:
    - "**.g.dart"
    - "**.freezed.dart"
    - "**_main.dart"
  # Define the paths of the folders in which to generate the index files
  libraries:
    - directory_path: lib
